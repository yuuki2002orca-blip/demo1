# このファイルは不要です。
# APIキーは一切必要ありません。
#
# 初回実行時にブラウザが開くので、各サービスに手動でログインしてください。
# ログイン情報は sessions/ フォルダにクッキーとして保存され、2回目以降は自動でログインされます。
#
#   sessions/claude_cookies.json  ← claude.ai のセッション
#   sessions/x_cookies.json       ← X (Twitter) のセッション
#   sessions/note_cookies.json    ← note.com のセッション
